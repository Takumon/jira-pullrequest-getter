{{#issues}}

## [{{key}}]({{url}})
{{summary}}

### 概要
{{ description }}

### モジュールとプルリクエスト
{{#modules}}
* [{{repository}}](http://github.com/{{owner}}/{{repository}})
  {{#pullRequests}}
  * [#{{pullRequestNumber}} {{name}}]({{url}})
  {{/pullRequests}}
{{/modules}}


### 修正ファイル
{{#modifiedPullRequestDetails}}
* [{{repository}}](http://github.com/{{owner}}/{{repository}})
  {{#files}}
  * {{filename}}
    * GitHub{{#if allPullRequestDiff}} [All Diff]({{{allPullRequestDiff}}}) {{/if}}
    {{#pullRequests}}
      * #{{pullRequestNumber}} [Diff]({{../../../key}}/{{../../repository}}/{{../filename}}.{{pullRequestNumber}}.{{status}}.patch.html) ({{status}}) [After PullRequest]({{url_after}})
    {{/pullRequests}}
    * SVN({{pullRequests.0.svnInfo.status}})
    {{#ifCond pullRequests.0.status 'added'}}
    {{!-- 追加時はSVNにもファイルがないため差分比較は存在しないので、リンクは表示しない --}}
    {{else}}
      {{#pullRequests.0.svnInfo.files}}
      * [Diff]({{../../../../key}}/{{../../../repository}}/{{../../filename}}.{{../pullRequestNumber}}.{{../status}}.patch.svn_{{@index}}.html) {{url}}
      {{/pullRequests.0.svnInfo.files}}
    {{/ifCond}}
  {{/files}}
{{/modifiedPullRequestDetails}}

{{/issues}}